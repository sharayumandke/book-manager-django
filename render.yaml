services:
  - type: web
    name: book-manager-django
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: |
      python manage.py makemigrations --noinput
      python manage.py migrate --noinput
      gunicorn myproject.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.11


